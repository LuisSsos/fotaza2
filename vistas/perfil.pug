extends layout

block contenido
  h2 #{usuario.nombre_usuario}
  p #{usuario.correo}
  br
  if usuario.biografia
    p #{usuario.biografia}
  br
  a.btn.btn-secondary(href='/') Volver
  a.btn.btn-danger(href='/auth/logout') Cerrar sesion